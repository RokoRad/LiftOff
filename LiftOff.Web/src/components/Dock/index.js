import React from 'react';
import Calibration from '../Calibration';
import Picker from '../Picker';

import './style.css';

export default ({}) => (
  <div className="dock">
    <Calibration />
    <Picker />
  </div>
);

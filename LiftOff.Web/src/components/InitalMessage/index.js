import React from 'react';
import './style.css';

const InitalMessage = ({}) => (
  <div className="inital-message">
    <a href="#" className="inital-message__text">
      Have an account? <span className="inital-message__text--bold">Login</span>
    </a>
  </div>
);

export default InitalMessage;
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  item: {
    flex: 1,
    width: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
  },
  inner: {
    width: 30,
    height: 30
  }
});
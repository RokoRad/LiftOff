import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 15,
    paddingTop: 5,
    marginBottom: 0,
    height: '60%'
  },
  full: {
    marginTop: '100%'
  }
});
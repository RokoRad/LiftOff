import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 15,
    paddingTop: 5,
    marginBottom: 0,
    height: '60%'
  },
  full: {
    marginTop: '100%'
  }
});

export default styles;
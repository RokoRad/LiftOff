import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 15,
    paddingTop: 5,
    paddingBottom: 50,
    height: '60%'
  }
});

export default styles;
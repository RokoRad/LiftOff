import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  image: {
    width: '100%',
    resizeMode: 'contain',
    height: 250,
    maxWidth: 230,
    marginLeft: 'auto',
    marginRight: 'auto',
    marginBottom: 10
  },
});
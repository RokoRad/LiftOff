import { StyleSheet } from 'react-native';
import vars from '../../config/vars.js';

export default StyleSheet.create({
  image: {
    position: 'absolute',
    right: 0,
    top: -150,
    bottom: 0,
    left: -100
  }
});

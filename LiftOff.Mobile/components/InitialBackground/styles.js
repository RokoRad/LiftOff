import { StyleSheet } from 'react-native';
import vars from '../../config/vars.js';

export default StyleSheet.create({
  image: {
    position: 'aboslute',
    top: 0,
    left: 0,
    right: 0, 
    bottom: 0
  }
});

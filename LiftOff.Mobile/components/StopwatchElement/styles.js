import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  inner: {
    backgroundColor: 'red',
    paddingTop: 20,
    paddingLeft: 10,
    paddingBottom: 20,
    paddingRight: 10,
    color: '#fff',
    fontSize: 40,
    width: 'auto'
  }
});

export default styles;
// includanje stylesheet opcije
import { StyleSheet } from 'react-native';

// kreirnje stilova za initalLogo
const styles = StyleSheet.create({
  image: {
    width: '100%',
    resizeMode: 'contain',
    height: 250,
    maxWidth: 230,
    marginLeft: 'auto',
    marginRight: 'auto',
    marginBottom: 10
  },
});

export default styles;
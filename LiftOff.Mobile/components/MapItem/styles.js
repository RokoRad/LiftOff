import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  item: {
    position: 'absolute', 
    right: 0, 
    width: 50, 
    height: 50, 
    backgroundColor: 'red', 
    zIndex: 999
  }
});

export default styles;
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  loginHolder: {
    backgroundColor: 'red',
    position: 'absolute',
    zIndex: 99,
    width: '100%',
    height: '25%',
  }
});

export default styles;
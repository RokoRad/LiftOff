import { StyleSheet } from 'react-native';
import vars from '../../config/vars.js';

export default StyleSheet.create({
  picker: {
    height: 40,
    width: '100%',
    color: vars.grey
  }
});

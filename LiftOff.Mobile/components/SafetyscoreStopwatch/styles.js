import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    backgroundColor: 'red',
    position: 'absolute',
    height: 80,
    width: '100%',
    display: 'flex',
    top: 0,
    left: 0
  }
});

export default styles; 
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 30,
    paddingTop: 20
  },
  scrollview: {
    height: '37%'
  }
});

export default styles;
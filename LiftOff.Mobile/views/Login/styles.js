import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  inputWrapper: {
    marginTop: 30,
    marginBottom: 40
  },
  loginWrapper: {
    position: 'relative',
    height: '100%',
    width: '100%'
  },
  loginBody: {
    position: 'absolute',
    bottom: 0,
    left: 0,
    width: '100%',
    height: 'auto'
  }
});

export default styles;
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  bothAligned: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    width: '80%',
    flexDirection: 'row'
  }
});
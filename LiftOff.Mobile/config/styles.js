import { StyleSheet } from 'react-native';
import vars from './vars.js';

export default StyleSheet.create({
  green: {
    color: vars.green
  },
  orange: {
    color: vars.orange
  },
  red: {
    color: vars.bad
  }
});
